/**
 * Central type exports.
 * Import domain types from this module rather than from scattered locations.
 *
 * @example
 *   import type { Student, AttendanceStatus } from "@/types";
 */

export type {
  Gender,
  AttendanceStatus,
  Student,
  Survey,
  AttendanceRecord,
} from "@/shared/types/domain";
